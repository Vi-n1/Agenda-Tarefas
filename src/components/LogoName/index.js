import React from 'react';
import { View, Text } from 'react-native';

import styles from './styles';

const LogoName = () => {
    return (
        <View style={styles.container}>
            <Text style={styles.LogoText}>LUH SALGADOS </Text>
        </View>
    );
};

export default LogoName;